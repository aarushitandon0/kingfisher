"""Shared plumbing: settings, structured logging, database session.

`core/` holds cross-cutting infrastructure only. Domain logic lives in
`pipeline/` (I/O), `models/` (learning) and `engine/` (pure functions).
"""
